<h1><%=t('comentarios')%></h1>
<br/>
<table>
  <tr>
    <th><%=t('tituloes')%></th>
    <th><%=t('titulofr')%></th>
    <th><%=t('textoes')%></th>
    <th><%=t('textofr')%></th>
    <th><%=t('created')%></th>
    <th><%=t('usuario')%></th>
    <th><%=t('equipo')%></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comentarios.each do |comentario| %>
  <tr>
     <td><%=comentario.tituloes%></td>
    <td><%=comentario.titulofr%></td>
    <td><%= truncate(comentario.textoes,:lenght=>25) %></td>
    <td><%= truncate(comentario.textofr,:lenght=>25) %></td>
      <td><%=comentario.created_at%></td>
      <td><%=comentario.usuarioforo.nombre%></td>
      <td><%=comentario.usuarioforo.equipo_id%></td>

    <td><%= link_to t('show'), comentario_path(comentario,:vista=>'gestion') %></td>
    <td><%= link_to t('edit'), edit_comentario_path(comentario,:vista=>'gestion') %></td>
   <%if current_user.tipo!='traductor'%>
    <td><%= link_to t('destroy'), comentario, :confirm => t('confirm'), :method => :delete %></td>
  <%end%>
  </tr>
<% end %>
</table>

<br />
<%if current_user.tipo=='traductor'%>
    <%=link_to t('back'),traductor_url%>
<%else%>
     <%=link_to t('back'),foro_path(@foro)%>
<%end%>
