<tr>
  <td> <%= pagina.tituloes %> </td>
  <td>  <%= pagina.titulofr %> </td>
  <td><%= link_to t('show'), pagina_path(pagina, :sitio_id=>sitio_id)%></td>
  <td><%= link_to t('edit'), edit_pagina_path(pagina, :sitio_id=>sitio_id )%></td>    
  
  <td><%= link_to t('destroy'), [pagina], :confirm => t('confirm'),  :method => :delete %></td>
 </tr>

