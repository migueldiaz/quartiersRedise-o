<h1><%=t('new')+' '+t('pagina')%></h1>

<%= render 'form' %>

<% if !current_user.tipo=='traductor'%>
<%= link_to t('back'),paginas_path %>
<%else%>
<%= link_to t('traductor'),traductor_url %>
<%end%>
